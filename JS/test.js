const myObject = {
    name: "James",
    city: "Columbus",
    vehicle: "car"
}


request.post(myObject);


applicationCache.requestHandler = (data) => {
    const username = data.username;
    const password = data.password;
}